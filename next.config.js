/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    TENANT_SLUG: 'iqranuraniacademy',
    TENANT_NAME: 'ইকরা নূরানী একাডেমি',
  },
}

module.exports = nextConfig